[project]
name = "stockmanager-desktop"
version = "0.1.0"
description = "Application desktop de gestion de stock (Python, PySide6, SQLAlchemy, SQLite)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
qt_api = "pyside6"
